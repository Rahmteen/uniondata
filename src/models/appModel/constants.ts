import type { AppState } from './types'

/**
 * @name AppConstants
 * @description Constants for the app model
 */

export const initialState: AppState = {
  loading: false,
  error: null,
  initialized: false,
}

